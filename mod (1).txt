داش من منظورم اینه من یه سیستم تو خونه دارم که به نت وصلش کردم و بعدش  توی روتر یا مودمم پورت فورواردینگ زدم و یه اسکریپت اس اس اچ توی سیگوین اجرا کردم که بتونم از راه دور به ترمینال دسترسی داشته باشم

بعدشم اینکه یه ای اس پی 32 با یه ماژول w5500
یه لن بهش میزنم با برنامه کاستومی که روی ای اس پی 32 ما هستش
اون لنو میزنم توی سیستم خونه من
توی اون اسکریپت سیگوین من این قابلیت اجرا میشه که بتونیم از وای فای و لن همزمان استفاده کنم
بهت میگم چرا

میرم سر یه سایت مخابراتی اریکسون و گوشیمو باز میکنم اس اس اچ بهش میزنم
منظورم سیستمم هست

و بعدش esp32 + w5500
خودمو وصل میکنم به یونیتم مثل bbu
به پورت LMT

بعدش هنوزکارمون تموم نشده
یه اکسس پوینت سرسایت برام درست میشه بهش وصل میشم

و از طریقش
باید وصل بشیم به سیستم خونمون ولی از طریق اون esp32 خونمون
که از طریق ip public ما این اتفاق میفته و میتونیم اتصال  پیدا کنیم

can we make a project with esp32 and  w5500 that can connect to a laptop using ethernet and make a connection that let esp32 use the port forwarding to connect to another switch in another location and city using another esp32 with w5500 and let the switch lan cable that is connected to the second esp32  with w5500 that  is tunneling a connection to our public address (port forwarding is active)

i explained complicated
with this explanation:
داداش نگاه کن ما یه نرم افزار داریم به اسم 
ericsson moshell

من تونستم از طریق این به صورت ریموت بهش وصل بشم
یعنی چی

یعنی یه لپ تاپ تو خونه دارم
به اینتنرنت وصل کردمش
یه اسکریپت اچرا کردم توی cygwin
که بتونم از طریق اون یه تانل یا به اصطلاحخ یه ssh
بزنم به به سیستمم که بیاد از طریق پورت فورواردینگ بیاد این اس اس اچ به اینترنت خونه من وصل  بشه
بعدش به صورت worldwide

بتونم بهش متصل بشم
بعدش میتونم از طریق گوشیم و هرسیستمی از طریق ای پیش وصل بشم از هرکجا ایران

ولی  خب به صورت پیشفرض ما از گوشی بهش وصل میشیم
مشکل ما اینه که میخوام به تجهیزات مخابراتی اریکسون که از طریق لن هستش وصل بشم

من میگم یه راهی داریم
ما میایم یه برد esp32
wroom 32 - devkitv1
میخریم

با w5500
از هر کدوم دو تا

بعدش میایم
یه کد روی یدونشون مینویسم که بیاد وصل بشه به سیستم توی خونه ما
از طریق  کابل لنی که به w5500
میخوره

وقتی به سیستممون وصل بشه باید
توسط یه اسکریپت که اجازه میده یه قابلیت بسازیم که بتونیم لن و وایفای رو همزمان استفاده کنیم
اقدام کنیم - حالا مهم نیست اون اسکریپت به زبان پایتونی نوشته شده یانه نیازم نیست بنویسی

حالا بهتره که خود برد ماهم بتونه اینو به سیستم بفهمونه که
باید لن و وای فای هم باید باهم فعال باشن بدون قطعی در وای فای

ببین اگر به صورت کلی نمیشه همچین دستوری به برد esp32
ما داد به صورت کلی فراموشش کن من با یه اسکریپت اوکیش میکنم

بعدش این برد ما باید یه پنل وب داشته باشه
بسیار خوشگل و زیبا طراحی شده باشه
با رنگ پذیری و طراحی بسیار عالی  و دارک
بدون هیچ عکسی
با عنوان LavaShell MoDroid
از چه طریقی میشه بهش دسترسی پیدا کرد؟
این آی پی 
10.44.22.1

قابلیت پسورد هم داشتته باشه و به صورت دیفالت باشه
lsmod
و بشه از طریق پنل عوضش کرد

قابلیت reboot هم باشه
تمامی عواملی که داخل پنل اجرا میشه و هرکاری که انجام میشه
چراغ آبی رنگ هم باید باهاش sync باشه و روشن بشه وقتی یکاری داره انجام میشه باید چشمک بزنه - وقتی روی idle هست همینطوری روشن بمونه

حجم فایلتم مهم نیست چی باشه
باید این کد طول
یه وای فای
and continue of the explanation soon... i mean u gotta get what i mean bro
tell me if its possible or not in one word. dont write anything like code my explanation isnt done yet

------------------

داداش نگاه کن ما یه نرم افزار داریم به اسم 
ericsson moshell

من تونستم از طریق این به صورت ریموت بهش وصل بشم
یعنی چی

یعنی یه لپ تاپ تو خونه دارم
به اینتنرنت وصل کردمش
یه اسکریپت اچرا کردم توی cygwin
که بتونم از طریق اون یه تانل یا به اصطلاحخ یه ssh
بزنم به به سیستمم که بیاد از طریق پورت فورواردینگ بیاد این اس اس اچ به اینترنت خونه من وصل  بشه
بعدش به صورت worldwide

بتونم بهش متصل بشم
بعدش میتونم از طریق گوشیم و هرسیستمی از طریق ای پیش وصل بشم از هرکجا ایران

ولی  خب به صورت پیشفرض ما از گوشی بهش وصل میشیم
مشکل ما اینه که میخوام به تجهیزات مخابراتی اریکسون که از طریق لن هستش وصل بشم

من میگم یه راهی داریم
ما میایم یه برد esp32
wroom 32 - devkitv1
میخریم

با w5500
از هر کدوم دو تا

بعدش میایم
یه کد روی یدونشون مینویسم که بیاد وصل بشه به سیستم توی خونه ما
از طریق  کابل لنی که به w5500
میخوره

وقتی به سیستممون وصل بشه باید
توسط یه اسکریپت که اجازه میده یه قابلیت بسازیم که بتونیم لن و وایفای رو همزمان استفاده کنیم
اقدام کنیم - حالا مهم نیست اون اسکریپت به زبان پایتونی نوشته شده یانه نیازم نیست بنویسی

حالا بهتره که خود برد ماهم بتونه اینو به سیستم بفهمونه که
باید لن و وای فای هم باید باهم فعال باشن بدون قطعی در وای فای

ببین اگر به صورت کلی نمیشه همچین دستوری به برد esp32
ما داد به صورت کلی فراموشش کن من با یه اسکریپت اوکیش میکنم

بعدش این برد ما باید یه پنل وب داشته باشه
بسیار خوشگل و زیبا طراحی شده باشه
با رنگ پذیری و طراحی بسیار عالی  و دارک
بدون هیچ عکسی
با عنوان LavaShell MoDroid
از چه طریقی میشه بهش دسترسی پیدا کرد؟
این آی پی 
10.44.22.1

قابلیت پسورد هم داشتته باشه و به صورت دیفالت باشه
lsmod
و بشه از طریق پنل عوضش کرد

قابلیت reboot هم باشه
تمامی عواملی که داخل پنل اجرا میشه و هرکاری که انجام میشه
چراغ آبی رنگ هم باید باهاش sync باشه و روشن بشه وقتی یکاری داره انجام میشه باید چشمک بزنه - وقتی روی idle هست همینطوری روشن بمونه

حجم فایلتم مهم نیست چی باشه
باید این کد طول
یه وای فای

 ┌──────────────┐        Internet (Public IP)       ┌───────────────┐
 │   Remote Site│                                   │   Home Setup  │
 │ -------------│                                   │---------------│
 │ ESP32 + W5500│◄─── Ethernet ───► Ericsson BBU/LMT│               │
 │ (Client Mode)│                                   │ ESP32 + W5500 │
 │ Wi-Fi (AP) ⇄ Phone SSH →→→→→→→→→→→→→→→→→→→→→→→→ │ (Server Mode) │
 │   (SSH to Home)                                  │ LAN to PC     │
 └──────────────┘                                   │ Port Forward  │
                                                    │  →  Public IP │
                                                    └───────────────┘


داداش نگاه کن ما یه نرم افزار داریم به اسم 
ericsson moshell

من تونستم از طریق این به صورت ریموت بهش وصل بشم
یعنی چی

یعنی یه لپ تاپ تو خونه دارم
به اینتنرنت وصل کردمش
یه اسکریپت اچرا کردم توی cygwin
که بتونم از طریق اون یه تانل یا به اصطلاحخ یه ssh
بزنم به به سیستمم که بیاد از طریق پورت فورواردینگ بیاد این اس اس اچ به اینترنت خونه من وصل  بشه
بعدش به صورت worldwide

بتونم بهش متصل بشم
بعدش میتونم از طریق گوشیم و هرسیستمی از طریق ای پیش وصل بشم از هرکجا ایران

ولی  خب به صورت پیشفرض ما از گوشی بهش وصل میشیم
مشکل ما اینه که میخوام به تجهیزات مخابراتی اریکسون که از طریق لن هستش وصل بشم

من میگم یه راهی داریم
ما میایم یه برد esp32
wroom 32 - devkitv1
میخریم

با w5500
از هر کدوم دو تا

بعدش میایم
یه کد روی یدونشون مینویسم که بیاد وصل بشه به سیستم توی خونه ما
از طریق  کابل لنی که به w5500
میخوره

وقتی به سیستممون وصل بشه باید
توسط یه اسکریپت که اجازه میده یه قابلیت بسازیم که بتونیم لن و وایفای رو همزمان استفاده کنیم
اقدام کنیم - حالا مهم نیست اون اسکریپت به زبان پایتونی نوشته شده یانه نیازم نیست بنویسی

حالا بهتره که خود برد ماهم بتونه اینو به سیستم بفهمونه که
باید لن و وای فای هم باید باهم فعال باشن بدون قطعی در وای فای

ببین اگر به صورت کلی نمیشه همچین دستوری به برد esp32
ما داد به صورت کلی فراموشش کن من با یه اسکریپت اوکیش میکنم

بعدش این برد ما باید یه پنل وب داشته باشه
بسیار خوشگل و زیبا طراحی شده باشه
با رنگ پذیری و طراحی بسیار عالی  و دارک
بدون هیچ عکسی
با عنوان LavaShell MoDroid
از چه طریقی میشه بهش دسترسی پیدا کرد؟
این آی پی 
10.44.22.1
توجه کن که داداش ما دو تا کد میخوایم که دو تا فریمور جدا هست
هردو نحو کارشون طبق توضیحتم متفاوت هست

آی پی برای سایت bts ما باید باشه
10.44.22.1
برای خونه باشه
10.44.11.1

قابلیت پسورد هم داشتته باشه و به صورت دیفالت باشه
lsmod
و بشه از طریق پنل عوضش کرد

قابلیت reboot هم باشه
Restore Board هم داشته باشه که همون ریست خودمونه تنظیمات برمیگده به حالت کارخانه
تمامی عواملی که داخل پنل اجرا میشه و هرکاری که انجام میشه
چراغ آبی رنگ هم باید باهاش sync باشه و روشن بشه وقتی یکاری داره انجام میشه باید چشمک بزنه - وقتی روی idle هست همینطوری روشن بمونه

حجم فایلتم مهم نیست چی باشه
باید این کد طولانی باشه که صددرصد هست

توی این وب پنل ما بتونیم رمز و اینارو تغییر بدیم ولی نزار کاربر SSID رو تغییر بده روی هردو فریمور

امنیت وای فاش بالاترین حد ممکن باشه
و امنیت فایروال یا فایروال خودمون رو داخل این پنل اضافه کن
من یکبار دیگه توضیح میدم ببین داش ما یه نرم افزاری داریم به اسم موشل که برای اریکسون هست که توی سیگوین اجرا میشه و اینکه برای اینکه بتونیم
بهش دسترسی داشته باشیم باید ویندوز داشته باشیم. من میخام کاری کنم بشه از راه دور بهش دسترسی داشته باشه و بتونیم مثل یه کابل لن چند کیلومتری از سایت بی تی اس تا خونمون کابلو به لپ تاپ وصل کنیم ولی این فرضی بود ما کابل نمیخوایم

از طریق esp32 + w5500 سر سایت
ما میتونیم بیایم از طریق پورتش کابلو به پورت LMT بیس بند خودمون یا هر یونیتی وصل کنیم

بعدش بیایم به اکسس پوینت ای اس پی وصل بشیم نت همراه خودمون رو هات اسپات  همزمان کنیم
و بعدش بیایم هات اسپات گوشیو به ای اس پی 32 وصل کنیم

و بعدش این برد توسعه ما میاد مثل یه ماژول  ssh عمل میکنه
و این اسکریپت ما:

#!/bin/bash
#!/usr/bin/env bash

# LavaShell MoDroid - SSH Remote Access Manager
# Fixed Host Key Persistence + SSH Welcome Message
# Created by Moein Nikchehre

set -e

# Colors and formatting
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
PURPLE='\033[0;35m'
CYAN='\033[0;36m'
WHITE='\033[1;37m'
BOLD='\033[1m'
NC='\033[0m'

# Configuration
CONFIG_DIR="$HOME/.lavashell"
CONFIG_FILE="$CONFIG_DIR/config"
DEFAULT_SSH_PORT=22
ALTERNATE_PORT=2222
CURRENT_PORT=$DEFAULT_SSH_PORT
IS_CYGWIN=false

# Global variables
CURRENT_SELECTION=0
MENU_ITEMS=()
SSH_STATUS="UNKNOWN"
PUBLIC_IP="Unknown"
LOCAL_IP="Unknown"
SSH_PID=""

# Detect environment
detect_environment() {
    if [[ $(uname -s) == CYGWIN* ]]; then
        IS_CYGWIN=true
        print_success "Cygwin environment detected"
    else
        print_error "This script is designed for Cygwin only!"
        exit 1
    fi
}

# ASCII Art
print_banner() {
    clear
    echo -e "${PURPLE}"
    cat << "BANNER"
    __                      _____ __         ____
   / /   ____ __   ______ _/ ___// /_  ___  / / /
  / /   / __ `/ | / / __ `/\__ \/ __ \/ _ \/ / / 
 / /___/ /_/ /| |/ / /_/ /___/ / / / /  __/ / /  
/_____/\__,_/ |___/\__,_//____/_/ /_/\___/_/_/   
    __  ___      ____             _     __       
   /  |/  /___  / __ \_________  (_)___/ /       
  / /|_/ / __ \/ / / / ___/ __ \/ / __  /        
 / /  / / /_/ / /_/ / /  / /_/ / / /_/ /         
/_/  /_/\____/_____/_/   \____/_/\__,_/
                                             
BANNER
    echo -e "${NC}"
    echo -e "${CYAN}┌────────────────────────────────────────────────────────────┐${NC}"
    echo -e "${CYAN}│${WHITE}    LavaShell MoDroid - V1 / Server ${CYAN}         │${NC}"
    echo -e "${CYAN}│${WHITE}    Moein Nikchehre - lsi.ct.ws${CYAN}              │${NC}"
    echo -e "${CYAN}└────────────────────────────────────────────────────────────┘${NC}"
    echo
}

# Utility functions
print_status() {
    echo -e "${BLUE}[INFO]${NC} $1"
}

print_success() {
    echo -e "${GREEN}[SUCCESS]${NC} $1"
}

print_warning() {
    echo -e "${YELLOW}[WARNING]${NC} $1"
}

print_error() {
    echo -e "${RED}[ERROR]${NC} $1"
}

print_header() {
    echo
    echo -e "${CYAN}${BOLD}$1${NC}"
    echo -e "${CYAN}──────────────────────────────────────────────────────────────${NC}"
}

print_menu_item() {
    local index=$1
    local text=$2
    local selected=$3
    
    if [ $selected -eq 1 ]; then
        echo -e " ${GREEN}❯${NC} ${BOLD}${text}${NC}"
    else
        echo -e "   ${text}"
    fi
}

# SSH Welcome Message Setup
setup_ssh_welcome() {
    local welcome_script="$HOME/.lavashell/ssh-welcome.sh"
    local bashrc="$HOME/.bashrc"
    local profile="$HOME/.profile"
    
    print_status "Setting up SSH welcome message..."
    
    mkdir -p "$HOME/.lavashell"
    
    # Create the welcome script
    cat > "$welcome_script" << 'EOF'
#!/bin/bash
# LavaShell MoDroid SSH Welcome - Auto-generated
# This shows when you connect via SSH (Termius, Termux, etc.)

if [ -n "$SSH_CONNECTION" ] && [ -z "$LAVASHELL_WELCOME_SHOWN" ]; then
    # Colors for welcome message
    BLUE='\033[0;34m'
    GREEN='\033[0;32m'
    WHITE='\033[1;37m'
    YELLOW='\033[1;33m'
    CYAN='\033[0;36m'
    PURPLE='\033[0;35m'
    RED='\033[0;31m'
    ORANGE='\033[0;33m'
    NC='\033[0m'
    
    # Function to show countdown
    countdown() {
        local seconds=$1
        while [ $seconds -gt 0 ]; do
            printf "\r${CYAN}Starting shell in %2d seconds...${NC}" $seconds
            sleep 1
            : $((seconds--))
        done
        printf "\r\033[K"
    }
    
    # Clear screen and show welcome
    clear
    echo -e "${BLUE}"
    echo "╔════════════════════════════════════════════════════════════╗"
    echo "║              Welcome to LavaShell MoDroid!                 ║"
    echo "║              Moein Nikchehre - V1 - Client                 ║"
    echo "║              Based on U3jit & ShupXQL Engine               ║"
    echo "║                   Website: lsi.ct.ws                       ║"
    echo "╚════════════════════════════════════════════════════════════╝"
    echo -e "${NC}"
    echo
    
    # Loading sequence with different colors and timings
    echo -e "${YELLOW}⏳ Loading u3LIB${NC}" && sleep 0.5
    echo -e "${GREEN}  ✓ lsjit Core libraries initialized${NC}"
    
    echo -e "${YELLOW}⏳ Loading u3MOS${NC}" && sleep 1
    echo -e "${GREEN}  ✓ MoShell .bash/loaders complete${NC}"
    
    echo -e "${CYAN}⏳ Patching MoShell Element Manager for Terminal Access${NC}" && sleep 1
    echo -e "${GREEN}  ✓ MOS-Conto +10 lsPKGs applied${NC}"
    
    echo -e "${PURPLE}⏳ Hardloading Modules for Termux, if exist${NC}" && sleep 1
    echo -e "${GREEN}  ✓ TERM modules checked${NC}"
    
    echo -e "${RED}⏳ Reserving MoD USU Parameters for Cyg & mintty${NC}" && sleep 3
    echo -e "${GREEN}  ✓ USU/LOD parameters reserved${NC}"
    
    echo -e "${GREEN}⏳ Finalizing Terminal *MoDlib-xway${NC}" && sleep 0.5
    echo -e "${GREEN}  ✓ LavaXWayland compatibility loaded${NC}"
    
    echo -e "${GREEN}⏳ Finalizing Terminal *MoDlib-ossh${NC}" && sleep 0.5
    echo -e "${GREEN}  ✓ OpenSSH extensions/self-lib enabled${NC}"
    
    echo -e "${GREEN}⏳ Finalizing Terminal *MoDlib-u3${NC}" && sleep 0.5
    echo -e "${GREEN}  ✓ U3 libraries + .lsclass integrated${NC}"
    
    echo -e "${BLUE}⏳ Finalizing Terminal *S/X+Y+Z/QL PROG;${NC}" && sleep 0.5
    echo -e "${GREEN}  ✓ u3hkey & MoDusuals activated${NC}"
    
    echo -e "${WHITE}✨ Done! Welcome $(whoami)${NC}" && sleep 1.5
    echo
    
    # Countdown before clearing
    countdown 3
    
    # Clear screen completely and set flag
    clear
    export LAVASHELL_WELCOME_SHOWN=1
fi
EOF

    chmod +x "$welcome_script"
    
    # Add to bashrc if not already there
    if ! grep -q "LAVASHELL_WELCOME" "$bashrc" 2>/dev/null; then
        echo >> "$bashrc"
        echo "# LavaShell MoDroid Welcome Message" >> "$bashrc"
        echo "source \"$welcome_script\"" >> "$bashrc"
        print_success "SSH welcome message configured in .bashrc"
    else
        print_status "SSH welcome already configured in .bashrc"
    fi
    
    # Also add to .profile for other shells
    if [ -f "$profile" ] && ! grep -q "LAVASHELL_WELCOME" "$profile" 2>/dev/null; then
        echo >> "$profile"
        echo "# LavaShell MoDroid Welcome Message" >> "$profile"
        echo "source \"$welcome_script\"" >> "$profile"
        print_success "SSH welcome message configured in .profile"
    fi
}

# Check if port is available
check_port_available() {
    local port=$1
    if netstat -an 2>/dev/null | grep -q ":$port.*LISTEN"; then
        return 1  # Port is in use
    else
        return 0  # Port is available
    fi
}

# Find what's using the port
find_port_usage() {
    local port=$1
    print_status "Checking what's using port $port..."
    
    if command -v netstat >/dev/null 2>&1; then
        netstat -ano | grep ":$port.*LISTEN" | head -1
    fi
}

# Kill process using port
kill_port_process() {
    local port=$1
    print_status "Attempting to free port $port..."
    
    if command -v netstat >/dev/null 2>&1; then
        local pid=$(netstat -ano | grep ":$port.*LISTEN" | awk '{print $5}' | head -1)
        if [ -n "$pid" ] && [ "$pid" != "0" ]; then
            print_warning "Killing process $pid using port $port"
            kill -9 $pid 2>/dev/null || taskkill /PID $pid /F 2>/dev/null
            sleep 2
        fi
    fi
}

# Network functions
get_network_info() {
    print_status "Detecting network configuration..."
    
    # Get local IP
    if command -v ipconfig >/dev/null 2>&1; then
        LOCAL_IP=$(ipconfig 2>/dev/null | grep -i "IPv4" | grep -oE '([0-9]{1,3}\.){3}[0-9]{1,3}' | grep -v '127.0.0.1' | head -1)
    fi
    
    [ -z "$LOCAL_IP" ] && LOCAL_IP="127.0.0.1"
    
    # Get public IP
    print_status "Getting public IP address..."
    PUBLIC_IP=$(curl -s --connect-timeout 5 https://api.ipify.org 2>/dev/null || echo "Unknown")
    
    print_success "Network detection complete"
}

# SSH Service Management
check_ssh_service() {
    if [ -f /tmp/lavashell_sshd.pid ]; then
        SSH_PID=$(cat /tmp/lavashell_sshd.pid 2>/dev/null)
        if [ -n "$SSH_PID" ] && kill -0 $SSH_PID 2>/dev/null; then
            SSH_STATUS="RUNNING"
            return 0
        else
            rm -f /tmp/lavashell_sshd.pid
        fi
    fi
    
    if pgrep -x sshd >/dev/null 2>&1; then
        SSH_STATUS="RUNNING"
    else
        SSH_STATUS="STOPPED"
    fi
}

# Create SSH configuration with dynamic port
create_ssh_config() {
    local config_file="$HOME/.LS-MoD/sshd_config"
    local port=${1:-$CURRENT_PORT}
    
    print_status "Creating SSH configuration for port $port..."
    
    mkdir -p "$HOME/.LS-MoD"
    
    # Backup existing config
    if [ -f "$config_file" ]; then
        cp "$config_file" "$config_file.backup.$(date +%Y%m%d_%H%M%S)"
    fi
    
    # Create config with specified port
    cat > "$config_file" << EOF
# LavaShell MoDroid - SSH Configuration
Port $port
Protocol 2
HostKey $HOME/.LS-MoD/ssh_host_rsa_key
HostKey $HOME/.LS-MoD/ssh_host_ecdsa_key  
HostKey $HOME/.LS-MoD/ssh_host_ed25519_key
PermitRootLogin no
AuthorizedKeysFile .LS-MoD/authorized_keys
PasswordAuthentication yes
PermitEmptyPasswords no
ChallengeResponseAuthentication no
X11Forwarding yes
PrintMotd yes
AcceptEnv LANG LC_*
Subsystem sftp /usr/sbin/sftp-server
AllowUsers $(whoami)
ListenAddress 0.0.0.0
UseDNS no
EOF

    chmod 600 "$config_file"
    print_success "SSH configuration created for port $port"
}

# Generate SSH host keys ONLY if they don't exist (FIXED)
generate_ssh_keys() {
    print_status "Checking SSH host keys..."
    
    mkdir -p "$HOME/.LS-MoD"
    
    local keys_missing=0
    
    # Check which keys are missing
    for key_type in rsa ecdsa ed25519; do
        key_file="$HOME/.LS-MoD/ssh_host_${key_type}_key"
        if [ ! -f "$key_file" ]; then
            keys_missing=1
            print_status "Generating $key_type host key..."
            ssh-keygen -t "$key_type" -f "$key_file" -N "" -q
            chmod 600 "$key_file"
            chmod 644 "${key_file}.pub"
            print_success "Generated $key_type host key"
        else
            print_status "$key_type host key already exists"
        fi
    done
    
    if [ $keys_missing -eq 0 ]; then
        print_success "All SSH host keys are present and ready"
    else
        print_success "Missing SSH host keys generated"
    fi
}

# Install and configure SSH server
install_ssh_server() {
    print_header "Installing OpenSSH Server"
    
    if ! command -v sshd >/dev/null 2>&1; then
        print_error "OpenSSH server not found!"
        echo
        echo -e "${YELLOW}To install OpenSSH in Cygwin:${NC}"
        echo -e "1. Run Cygwin setup.exe"
        echo -e "2. Search for 'openssh' package" 
        echo -e "3. Select openssh for installation"
        echo
        echo -e "${BOLD}After installation, run this script again.${NC}"
        echo
        read -n1 -r -p "Press any key to return to menu..."
        return 1
    fi
    
    print_success "OpenSSH Server is installed"
    
    # Configure SSH with current port
    create_ssh_config $CURRENT_PORT
    generate_ssh_keys
    
    # Setup SSH welcome message
    setup_ssh_welcome
    
    # Configure Windows Firewall for current port
    print_status "Configuring Windows Firewall for port $CURRENT_PORT..."
    netsh advfirewall firewall delete rule name="LavaShell SSH" 2>/dev/null || true
    netsh advfirewall firewall add rule name="LavaShell SSH" dir=in action=allow protocol=TCP localport=$CURRENT_PORT 2>/dev/null || print_warning "Could not configure firewall"
    
    print_success "SSH server setup completed"
    return 0
}

# Start SSH service with port management
start_ssh_service() {
    print_header "Starting SSH Service"
    
    # Stop any existing service first
    stop_ssh_service
    sleep 2
    
    # Check if default port is available
    if ! check_port_available $CURRENT_PORT; then
        print_warning "Port $CURRENT_PORT is already in use!"
        find_port_usage $CURRENT_PORT
        
        echo
        echo -e "${BOLD}Options:${NC}"
        echo -e "1. Try to free port $CURRENT_PORT"
        echo -e "2. Use alternative port $ALTERNATE_PORT"
        echo -e "3. Cancel and return to menu"
        echo
        
        read -p "Choose option (1/2/3): " choice
        case $choice in
            1)
                kill_port_process $CURRENT_PORT
                if check_port_available $CURRENT_PORT; then
                    print_success "Port $CURRENT_PORT is now free"
                else
                    print_error "Could not free port $CURRENT_PORT, using alternative port"
                    CURRENT_PORT=$ALTERNATE_PORT
                    create_ssh_config $CURRENT_PORT
                fi
                ;;
            2)
                CURRENT_PORT=$ALTERNATE_PORT
                create_ssh_config $CURRENT_PORT
                print_success "Switched to port $CURRENT_PORT"
                ;;
            3)
                return 1
                ;;
            *)
                print_error "Invalid choice, using alternative port"
                CURRENT_PORT=$ALTERNATE_PORT
                create_ssh_config $CURRENT_PORT
                ;;
        esac
    fi
    
    # Ensure keys and config are ready (DON'T regenerate keys every time)
    generate_ssh_keys  # This only generates missing keys now
    create_ssh_config $CURRENT_PORT
    
    # Test configuration
    print_status "Testing SSH configuration for port $CURRENT_PORT..."
    if /usr/sbin/sshd -t -f "$HOME/.LS-MoD/sshd_config"; then
        print_success "SSH configuration test passed"
    else
        print_error "SSH configuration test failed"
        return 1
    fi
    
    # Start SSH daemon
    print_status "Starting SSH daemon on port $CURRENT_PORT..."
    /usr/sbin/sshd -D -f "$HOME/.LS-MoD/sshd_config" &
    SSH_PID=$!
    
    # Wait for startup
    sleep 3
    
    if kill -0 $SSH_PID 2>/dev/null; then
        echo $SSH_PID > /tmp/lavashell_sshd.pid
        SSH_STATUS="RUNNING"
        print_success "SSH daemon started successfully (PID: $SSH_PID)"
        
        # Verify it's listening
        sleep 2
        if netstat -an 2>/dev/null | grep -q ":$CURRENT_PORT.*LISTEN"; then
            print_success "SSH server is listening on port $CURRENT_PORT"
        else
            print_warning "SSH process running but port $CURRENT_PORT not listening"
        fi
    else
        print_error "SSH daemon failed to start on port $CURRENT_PORT"
        return 1
    fi
    
    return 0
}

# Stop SSH service
stop_ssh_service() {
    print_header "Stopping SSH Service"
    
    # Stop using PID file
    if [ -f /tmp/lavashell_sshd.pid ]; then
        local pid=$(cat /tmp/lavashell_sshd.pid 2>/dev/null)
        if [ -n "$pid" ] && kill -0 $pid 2>/dev/null; then
            kill $pid 2>/dev/null && print_status "Stopped SSH daemon (PID: $pid)"
        fi
        rm -f /tmp/lavashell_sshd.pid
    fi
    
    # Kill any remaining sshd processes
    pkill -x sshd 2>/dev/null && print_status "Killed remaining SSH processes"
    sleep 2
    
    check_ssh_service
    
    if [ "$SSH_STATUS" = "STOPPED" ]; then
        print_success "SSH service stopped successfully"
    else
        print_warning "SSH service may still be running"
    fi
}

# Setup Router Mode
setup_router_mode() {
    print_header "Router/Modem Mode Setup"
    
    echo -e "${BOLD}${GREEN}Router/Modem Mode Selected${NC}"
    echo
    echo -e "This mode works when you have access to router settings."
    echo
    
    # Install and start SSH
    install_ssh_server
    if [ $? -eq 0 ]; then
        start_ssh_service
    fi
    
    if [ "$SSH_STATUS" = "RUNNING" ]; then
        show_router_instructions
    fi
}

# Show router instructions
show_router_instructions() {
    print_header "Router Mode Connection Instructions"
    
    echo -e "${BOLD}${GREEN}Step 1: Port Forwarding in Router${NC}"
    echo
    echo -e "Access your router admin (usually 192.168.1.1 or 192.168.0.1)"
    echo -e "Set up port forwarding:"
    echo -e "  • External Port: $CURRENT_PORT"
    echo -e "  • Internal Port: $CURRENT_PORT" 
    echo -e "  • Internal IP: ${LOCAL_IP}"
    echo -e "  • Protocol: TCP"
    echo
    
    echo -e "${BOLD}${GREEN}Step 2: Connect From Remote Device${NC}"
    echo
    if [ "$CURRENT_PORT" = "22" ]; then
        echo -e "From another computer/phone, use:"
        echo -e "  ${GREEN}ssh $(whoami)@${PUBLIC_IP}${NC}"
    else
        echo -e "From another computer/phone, use:"
        echo -e "  ${GREEN}ssh -p $CURRENT_PORT $(whoami)@${PUBLIC_IP}${NC}"
        echo -e "${YELLOW}Note: Use -p $CURRENT_PORT because we're not using default port 22${NC}"
    fi
    echo
    echo -e "Use your Windows password when prompted."
    echo
    
    echo -e "${BOLD}${GREEN}Step 3: Fix Host Key Warning (One Time)${NC}"
    echo
    echo -e "If you get 'Host key verification failed' in Termux:"
    echo -e "  ${GREEN}ssh-keygen -R $PUBLIC_IP -p $CURRENT_PORT${NC}"
    echo
    echo -e "Or to disable host key checking (less secure):"
    echo -e "  ${GREEN}ssh -o StrictHostKeyChecking=no -p $CURRENT_PORT $(whoami)@$PUBLIC_IP${NC}"
    echo
    
    echo -e "${BOLD}${YELLOW}Welcome Message:${NC}"
    echo -e "When you connect via SSH, you'll see a colorful welcome message"
    echo -e "that will automatically clear and give you terminal access."
    echo
    
    echo -e "${BOLD}${YELLOW}Troubleshooting:${NC}"
    echo -e "• If connection fails, check:"
    echo -e "  1. Port forwarding is correct for port $CURRENT_PORT"
    echo -e "  2. Windows Firewall allows port $CURRENT_PORT"
    echo -e "  3. Router restarted after changes"
    echo
}

# Test connection
test_connection() {
    print_header "Testing Connection"
    
    if [ "$SSH_STATUS" != "RUNNING" ]; then
        print_error "SSH server is not running"
        return 1
    fi
    
    print_status "Testing local connection..."
    if timeout 10s ssh -o ConnectTimeout=5 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -p $CURRENT_PORT localhost "echo 'Local test successful'" 2>/dev/null; then
        print_success "Local SSH connection working on port $CURRENT_PORT"
    else
        print_warning "Local connection test failed (normal for first connection)"
    fi
    
    print_status "Checking port $CURRENT_PORT..."
    if netstat -an 2>/dev/null | grep -q ":$CURRENT_PORT.*LISTEN"; then
        print_success "Port $CURRENT_PORT is listening"
    else
        print_error "Port $CURRENT_PORT is not listening"
        return 1
    fi
    
    echo
    echo -e "${BOLD}Connection Summary:${NC}"
    echo -e "Local IP: $LOCAL_IP"
    echo -e "Public IP: $PUBLIC_IP"
    echo -e "SSH Port: $CURRENT_PORT"
    echo -e "SSH Status: $SSH_STATUS"
    echo
    
    echo -e "${GREEN}Use this command from remote:${NC}"
    if [ "$CURRENT_PORT" = "22" ]; then
        echo -e "ssh $(whoami)@$PUBLIC_IP"
    else
        echo -e "ssh -p $CURRENT_PORT $(whoami)@$PUBLIC_IP"
    fi
    
    echo
    echo -e "${YELLOW}When you connect, you'll see the LavaShell welcome message!${NC}"
    echo
}

# Show system info
show_system_info() {
    print_header "System Information"
    
    echo -e "${BOLD}Hostname:${NC} $(hostname 2>/dev/null || echo 'Unknown')"
    echo -e "${BOLD}Username:${NC} $(whoami)"
    echo -e "${BOLD}Local IP:${NC} $LOCAL_IP"
    echo -e "${BOLD}Public IP:${NC} $PUBLIC_IP"
    echo -e "${BOLD}SSH Port:${NC} $CURRENT_PORT"
    
    case "$SSH_STATUS" in
        "RUNNING") echo -e "${BOLD}SSH Status:${NC} ${GREEN}RUNNING${NC}" ;;
        "STOPPED") echo -e "${BOLD}SSH Status:${NC} ${RED}STOPPED${NC}" ;;
        *) echo -e "${BOLD}SSH Status:${NC} ${YELLOW}UNKNOWN${NC}" ;;
    esac
    
    if command -v sshd >/dev/null 2>&1; then
        echo -e "${BOLD}SSH Server:${NC} ${GREEN}Installed${NC}"
    else
        echo -e "${BOLD}SSH Server:${NC} ${RED}Not Found${NC}"
    fi
    
    if netstat -an 2>/dev/null | grep -q ":$CURRENT_PORT.*LISTEN"; then
        echo -e "${BOLD}Port $CURRENT_PORT:${NC} ${GREEN}Listening${NC}"
    else
        echo -e "${BOLD}Port $CURRENT_PORT:${NC} ${RED}Not Listening${NC}"
    fi
    
    # Show host key status
    echo
    echo -e "${BOLD}SSH Host Keys:${NC}"
    for key_type in rsa ecdsa ed25519; do
        key_file="$HOME/.LS-MoD/ssh_host_${key_type}_key"
        if [ -f "$key_file" ]; then
            echo -e "  $key_type: ${GREEN}Exists${NC}"
        else
            echo -e "  $key_type: ${RED}Missing${NC}"
        fi
    done
    
    # Show welcome message status
    echo
    echo -e "${BOLD}Welcome Message:${NC}"
    if [ -f "$HOME/.lavashell/ssh-welcome.sh" ]; then
        echo -e "  Status: ${GREEN}Installed${NC}"
        echo -e "  Location: $HOME/.lavashell/ssh-welcome.sh"
    else
        echo -e "  Status: ${RED}Not Installed${NC}"
    fi
}

# Regenerate SSH host keys (only when needed)
regenerate_ssh_keys() {
    print_header "Regenerating SSH Host Keys"
    
    echo -e "${YELLOW}This will regenerate ALL SSH host keys.${NC}"
    echo -e "${RED}All remote clients will get 'Host key changed' warning!${NC}"
    echo
    read -p "Are you sure? (y/N): " confirm
    if [[ $confirm =~ ^[Yy]$ ]]; then
        print_status "Removing old host keys..."
        rm -f "$HOME/.LS-MoD/ssh_host_"* 2>/dev/null || true
        
        print_status "Generating new host keys..."
        for key_type in rsa ecdsa ed25519; do
            key_file="$HOME/.LS-MoD/ssh_host_${key_type}_key"
            ssh-keygen -t "$key_type" -f "$key_file" -N "" -q
            chmod 600 "$key_file"
            chmod 644 "${key_file}.pub"
            print_success "Generated $key_type host key"
        done
        
        print_success "SSH host keys regenerated"
        echo
        echo -e "${YELLOW}Note: Remote clients need to remove old host key:${NC}"
        echo -e "ssh-keygen -R $PUBLIC_IP -p $CURRENT_PORT"
    else
        print_status "Key regeneration cancelled"
    fi
}

# Fix SSH issues
fix_ssh_issues() {
    print_header "Fixing SSH Issues"
    
    print_status "Stopping SSH service..."
    stop_ssh_service
    
    print_status "Checking port availability..."
    if ! check_port_available $CURRENT_PORT; then
        print_warning "Port $CURRENT_PORT is in use, switching to $ALTERNATE_PORT"
        CURRENT_PORT=$ALTERNATE_PORT
    fi
    
    print_status "Ensuring SSH keys exist..."
    generate_ssh_keys
    
    print_status "Recreating SSH config for port $CURRENT_PORT..."
    create_ssh_config $CURRENT_PORT
    
    print_status "Setting up welcome message..."
    setup_ssh_welcome
    
    print_status "Starting SSH service..."
    if start_ssh_service; then
        print_success "SSH issues fixed successfully on port $CURRENT_PORT"
    else
        print_error "Failed to fix SSH issues"
    fi
}

# Interactive menu system
show_main_menu() {
    MENU_ITEMS=(
        "Setup Router/Modem Mode"
        "Start SSH Service"
        "Stop SSH Service"
        "Test Connection"
        "Show System Info"
        "Fix SSH Issues"
        "Regenerate SSH Host Keys"
        "Exit"
    )
}

draw_menu() {
    print_banner
    
    # Show quick status
    echo -e "${BOLD}Status:${NC} SSH: $SSH_STATUS | Port: $CURRENT_PORT | Local IP: $LOCAL_IP"
    echo
    
    print_header "Main Menu"
    for i in "${!MENU_ITEMS[@]}"; do
        if [ $i -eq $CURRENT_SELECTION ]; then
            print_menu_item "$i" "${MENU_ITEMS[$i]}" 1
        else
            print_menu_item "$i" "${MENU_ITEMS[$i]}" 0
        fi
    done
    
    echo
    echo -e "${YELLOW}Use ↑↓ arrows to navigate, Enter to select, Q to exit${NC}"
}

handle_keypress() {
    while true; do
        draw_menu
        read -rsn1 key
        case "$key" in
            $'\x1b')  # Escape sequence
                read -rsn2 -t 0.1 key2
                case "$key2" in
                    '[A') # Up arrow
                        CURRENT_SELECTION=$(( (CURRENT_SELECTION - 1 + ${#MENU_ITEMS[@]}) % ${#MENU_ITEMS[@]} ))
                        ;;
                    '[B') # Down arrow  
                        CURRENT_SELECTION=$(( (CURRENT_SELECTION + 1) % ${#MENU_ITEMS[@]} ))
                        ;;
                esac
                ;;
            '') # Enter key
                execute_menu_option
                ;;
            'q'|'Q') # Q key
                echo
                print_status "Thank you for using LavaShell MoDroid!"
                exit 0
                ;;
        esac
    done
}

execute_menu_option() {
    case $CURRENT_SELECTION in
        0) # Router Mode
            setup_router_mode
            ;;
        1) # Start SSH
            start_ssh_service
            ;;
        2) # Stop SSH
            stop_ssh_service
            ;;
        3) # Test Connection
            test_connection
            ;;
        4) # System Info
            show_system_info
            ;;
        5) # Fix Issues
            fix_ssh_issues
            ;;
        6) # Regenerate Keys
            regenerate_ssh_keys
            ;;
        7) # Exit
            echo
            print_status "Thank you for using LavaShell MoDroid!"
            exit 0
            ;;
    esac
    
    echo
    read -n1 -r -p "Press any key to continue..."
}

# Check dependencies
check_dependencies() {
    if ! command -v sshd >/dev/null 2>&1; then
        print_error "OpenSSH not found. Please install via Cygwin setup."
        echo
        echo -e "Run Cygwin setup.exe and install 'openssh' package"
        exit 1
    fi
}

# Main initialization
init_system() {
    print_banner
    print_status "Initializing LavaShell MoDroid..."
    
    detect_environment
    check_dependencies
    get_network_info
    check_ssh_service
    
    # Check initial port availability
    if ! check_port_available $CURRENT_PORT; then
        print_warning "Port $CURRENT_PORT is in use, switching to $ALTERNATE_PORT"
        CURRENT_PORT=$ALTERNATE_PORT
    fi
    
    mkdir -p "$CONFIG_DIR"
    print_success "System ready - Using port $CURRENT_PORT"
    sleep 1
}

# Signal handlers
trap 'echo; print_status "Interrupted. Continuing..."; show_main_menu' SIGINT

# Main execution
main() {
    init_system
    show_main_menu
    handle_keypress
}

# Run main function
main "$@"

این کد ماست....
باید به زودی قابلیت های جدید بهش اضافه کنیم

ما میخوایم اینم یه آپشنی داشته باشه. ما میخوایم این دوتا ای س پی 32 ما یکی تو خونه یکی سر سایت مخابراتی بتونه ارتباط بگیره که بتونه این لن سر سایت مارو تانل کنه و وصلش کنه به سیستم ما توی خونه که
باید وای فای و اترنت باهم همزمان اتصال داشت باشن.

این باعث میشه وقتی من با ای پی پابلیک خودم به ssh میزنم
و وصل میشم به سیگوین باعث میشه که هم بتونم به صورت از راه دور دسترسی داشته باشم به موشل و همچین فوق العاده مهم تر از اون لن سر سایت ما مثل این میمونه که توی لپ تاپ خونه ما وصل شده که سیگوین باهاش ارتباط بگیره و اتصال پیدا کنه

ما به صورت کلی دو تا فریمور میخوایم برای دو تا  ای اس پی سی و دو و دو تا w5500

esp32 + w5500 یدونش سر سایته
esp32 + w5500 اینم توی خونه ماست

هردوشون لنشون به یه جا وصل میشه یدونش به یونیت که پورت ال ام تی هست و دیگری به پورت لپ تاپ ما

این کانکشن باید همیشه سینک بشه و اپدیت بشه 
مشکلمون هم سرعت نیست هیچ مشکلی در این موضع نداریم.

ما به این دو تا برد توسعه و ماژول میگیم جمعا باکس
باکس توی خونه ما باید از طریق اینترنت خونه ما که پورت فورواردینگ 2222 رو انجام میده از طریق ای پی پابلیک
بزاره ما به اس اس اچ اتصال پیدا کنیم


و سر سایت ما اینترنت گوشیمونو هات اسپات میکنیم به ای اس پی خودمون که باکس سایت ما حساب میشه
و بعدش لن رو وصل میکنیم به ال ام تی پورت یا یونیت اریکسون خودمون که بتونه دیتا و به صورت کلی یه لن رو انتقال بده و مثل یه لن واقعی بیاد با استفاده از ای پی پابلیک ما تانل کنه بفرسته سمت خونه ما
ما فقط اینترنت هات اسپات رو برای داشتن اینترنت استفاده میکنیم
با استفاده از nat یا هرچی
باید کاری کنی که بشه به ای پی خونه وصل بشیم و اینترنت لوکال اون ال ام تی رو و شبکشو بفرستیم سمت باکس و باکس ما دریافت کنه و بفرسته سمت لن و لن بدتش به سیستم ما و ویندوز بخونتش توی خونه و سیگوین به یونیت وصل بشه و اینکه
ما سر سایت با گوشی یه اس اس اچ بزنیم و راحت به یونیتمون از راه دور دسترسی داریم

امیدوارم منظورمو کامل رسونده باشم.
داداش بزار یه سری چیزارو اینجا روشن کنم
اول اینکه ما سرعت فوق العاده زیادی نمیخوام چون خوب موشله دیگه فقط کامند میزنیم کار خاصی نیست
دسترسی کامند زدنشم که ما از روترمون و سیستممون میاد بیرون کاری با اون نداریم

آقا من منظورم اینه که ما یه باکس داریم که توش دوباره ای اس پی سی و دو و دبیلو 5500 داره منظورم اینه که بیاد اون کانکشن لن یونیت اریکسون ال ام تی رو انتقال بده به سیستم ما تو خونه که به یونیت اتصال پیدا کنیم

واقعا فکر کنم بشه با  esp32 w5500 اینکارو انجام بدیم

___________________________________________________________________________

سلام داداش خوبی؟
ببین داداش یه پروژه خیلی بزرگ داریم با اینکه جمع بندیش سادست ولی زیرساخت و یه پروژه پیچیده ای هست

نامش: LavaShell MoDroid
مخفف lsmod
سازنده: MOEIN NIKCHEHRE
ورژن (هردو فریمور متفاوت هستن): H0#1 برای خانه
S0#1
ازت میخوام اول توضیحات پایه رو براش بدیم

ابتدا پیش نیاز های این پروژه فیزیکی و نرم افزاری:

ESP32 - ۲ عدد
W5500 برد آبی SPI - ۲ عدد

خب اصلا هدف پروژمون توی چیه؟
این پروژه میتونه کاری کنه که ما یه پورت شبکه یه دستگاهرو کانکشنشو از طریق نقطه اتصال به یه شبکه تو خونمون از طریق IP که توی یه محیط لینوکسی CYGWIN هست وارد کنیم.

این توضیح سادش بود برات بهتر توضیح میدم
توپولوژی نحوه کار با این پروژه رو میگم که بتونی درک و استدلال نسبت به پروژه رو داشته باشی

خب داداش فرض کن ممد میخواو بره سر سایت مخابراتی که تجهیزات شبکه و مخابراتیش برای برند اریکسون هستن. معمولا تجهیزات اریکسون یه پورتی دارن به اسم LMT مثلا توی یونیت BBU یا BASEBAND UNIT یه پورتی هست به اسم LMT مثل پورت کنسول سیسکو میمونه ولی این از زمین تا آسمون باهاش فرق داره

خب اول اینکه LMT چیه
LMT = LOCAL MANAGEMENT TERMINAL

هستش...
یه پورتی هست توی اون تجهیزات که من میخوام بیام از طریق دو تا کد جدا برای فریمورمون استفاده کنم... 
برای همین دو تا ESP32 و W5500 خریدیم...
خب بریم سر ادامه مطلب
اره داش این پورت LMT رو من میخوام کانکشنو تانل کنم. یعنی چی ؟
ببین داش من موقعی که سر سایت میرم باید لپ تاپ ببرم و با لن وصل بشم به LMT و بعد وارد سیگوین بشم

و به این شکل وصل میشم به نرم افزار MOSHELL که نرم افزار ERICSSON هست برای کانفیگ کردن تجهیزات و ...

من یه اسکریپت برای سیگوین درست کرده بودم که میتونه یه کانکشن SSH درست کنه که از طریق ای پی پابلیک خودمون و پورت فورواردینگ پورت ۲۲۲۲ راحت از هرکجای ایران بهش وصل بشم با استفاده از این کامند
ssh -p (ip)
حالا پلتفرمی که ازش استفاده میکنیم اسمش هست ترمیوس که کارمون رو راحت تر میکنه...

این هیچی... حالا به زودی قراره برای همین یه قابلیت اضافه کنیم به اسکریپت که بتونه کاری کنه که اترنت + وای فای باهم توی سیستم کار کنن. + و این ماژول و آپشن قابلیت گرفتن شبکه کانکشن اترنت اون سایت به صورت تانل توی esp32 خونمون و ماژول w5500 فراهم بشه تا مثل این بمونه یه لن از خونه کشیدیم تا سایت ولی در واقع داریم به شبکه خونمون تانل میکنیم...

یکم پیچیده گفتم بیا بهت توپولوژی عملی این پروژه رو بگم...

داشتم میگفتم. ممد میره سر سایت گوشیشو باز میکنه بعدش میاد باکس ما (منطورم از باکس اینه که ماژول w5500 و esp32 ما هست که توی یه جعبه برای راحتی کار گذاشتیم) رو میزنه به برق و بعدش میاد به اکسس پوینت اون باکس ما سر سایت وصل میشه

بعد از وصل شدن میاد وارد پنل باکس ما میشه
وقتی واردش شد میاد IP روتر/مودم ما که آی پی پابلیک داره رو وارد میکنه بعدش پورت رو مینویسه (داداش یه نکته ای: اگر مشکلی پیش نمیاد روی ۲۲۲۲ باشه چون باید این کانکشن تانل شده اترنت سایت موشل بهش دسترسی بهینه ای داشته باشه و بشناستش)

داشتم میگفتم... بعدش رمز رو میزنه که توی اسکریپت تعریف شده رمز SSH ما رمز خود سیستم هست و اینکه توجه کن ما داریم کانکشن SSH تانل نمیکنیم. در اصل داریم کاری میکنیم پورت LMT سایت کانکشنش تانل بشه به سمت سیستم ما توی خونه که با اسکریپت خودمون باید اترنت و وای فای ادغام بشه شبکشون که هم تانل SSH ما برای سیگوین وصل باشه که از موشل استفاده کنیم و هم اینترنت که کانکشن سایت توی لپ تاپ یا سیستم توی خونه ما برای موشل متصل باشه.

بعد از اینکه رمزو زد این اطلاعاتش باید سیو بشه توی حافظه esp32

ولی قبل اینکه کاربر وصل بشه به کانکشن توی خونه ما باید توی پنل بره و یه کانکشن وای فای جدید اضافه کنه که هات اسپات گوشیشو میتونه اد کنه

این شکلی 
اسمشو مینیوسه و با پسوردش (قابلیت اسکن شبکه های اطراف هم بزار ولی بزار به صورت دیفالت کاربر رمز و ssid رو وارد کنه به بعدا esp32 بهش وصل بشه، ایناهم باید توی حافظه به صورت رمزگشایی شده مثل تمامی اطلاعات دیگه ذخیره بشه)

بعدش باید بیاد از اکسس پوینت باکس ما دیسکانکت بده بعد هات اسپات گوشیشو روشن کنه که داده همراهش وصله، و یه چیزی از لحظه ای esp32 روشن شد باید به صورت مداوم تلاش کنه به اون وای فایی که برای اتصال براش ست شده متصل بشه. یعنی من میرم سر سایت دیگه نیاز نباشه پنلو باز کنم هم برای وای فای خودمون و هم برای اتصال به سیستم خونه و تشکیل یه کانکشن تانل برای اترنت. یعنی وقتی زدم به برق بعد از بوت شدن و راه اندازی همچی باید سیستم تشخیص بده اول به اینترنت هات اسپات گوشیم وصل بشه و بعد به کانکشن که اسکریپت برای رد و بدل کردن کانکشن اترنتمون استفاده میشه.

خب این مرحله تموم شد حالا اقا ممد میاد یه لن برمیداره میزنه به یه سرشو به LMT یه سرشو میزنه به خود W5500 
بعدش باید ارتباط و کانکشن و تمامی داده های پورت LMT مثل یه پورت اترنت از راه دور بیاد از طریق ای پی پابلیک ما عبور کنه و به اسکریپت ما برسه و کانکشن اترنت مارو تشکیل بده و منی که سر سایتم گوشیمو راحت برم توی ترمیوس با ای پی پابلیکم وصل بشم به سیستمم توی خونه که SSH هستش و راحت اونجا من میتونم به موشل وصل بشم که میبینم اونجا یونیت من داره نشون داده میشه مثلا DUS یا BBU و مثل اینه که یه لن کشیدیم از سر سایت پورت LMT رو زدیم به پورت اترنت لپ تاپ ... ولی اینکارو به صورت تانل از طریق اتصال به شبکه توی خونمون انجام دادیم. و داده رو به سمت خونه ارسال میکنیم در اصل کانکشنو ارسال میکنیم

نکات بسیار مهم:
باید پنل ما یه صفحه لاگین داشته باشه...
فقط باید رمز داشته باشه
به صورت دیفالت هست (بعد فلش کردن دوباره که تمامی اطلاعات پاک میشه و ریست کردن تمامی اطلاعات از طریق پنل که یه آپشن هست ) 
lsmod-superadmin
و باید توی پنل ما قابلیت تغییرش وجود داشته باشه...

بعدش اینکه پنل ما باید بسیار ظاهر مدرن و دارک داشته باشه به اصطلاح گرادیانت
ولی زیاد سنگین نباشه از نظر انیمیشنی
پنل ادمین ما باید بسیار مدرن و زیبا طراحی شده باشه با تب های متنوع مثل Routing Firewall و ... (به هیچ عنوان توی اسم این تب و پیج ها از ایموجی استفاده نکن هرچیزی اسم ساده قشنگی داره)

توجه کن پنل ادمین ما سمت چپ باید تب و پیج ها اسمشون با عنوان زیر قسمتشون و فونت های متفاوت مثل بولد و حالت عادی باشه یا نازک

توجه کن که سمت چپ ما تب و پیج ها هستن نباید اون علامت دراز scroll sidebar باشه (منطورم اون لاین سفیده برای اسکرول) برای قشنگی پنل میگم.

چه تب ها و پیج هایی باید داشته باشیم
قسمت overview که به صورت کلی یه قسمت مانیتورینگ برای رد و بدل شدن اطلاعات به صورت graph و وضیعت اتصالاا و اطلاعات پیشرفته تر از حرفای من باشه ، مقدار حافظه باقیمونده از esp32 نشون داده بشه پهنای باند رو به صورت ثانوی نشون بده اگر کسی به اکسس پوینت وصل نبود وب سرور رو ببنده که از منابع esp32 کمتر استفاده بشه... کلا میخوام پنل یه ظاهر و آپشن های صنعتی و اتوماسیون شکل داشته باشه ولی lightweight که esp32 بتونه بکشه. (به هیچ عنوان محیط ساده نداشته باشه)

تب و پیچ هایی با اسم کانفیگوریشن که اطلاعات کانکشن خودمون رو تعریف و ذخیره میکنیم. و تنظیمات اتصال به اینترنت توی تب جدا باشه ولی دقت کن که تست اینترنت هم داشته باشه با ping گرفتن
قسمت هایی مثل Firewall بشه رولز ها و ... تعریف کرد با قسمت بندی های متفاوت و خودت بر عهده خودت هرآپشنی که به صورت واقعی کار میکنه رو تنظیم کن براش برای routing هم همینطور

یه تب هم بزار که بشه Reboot کرد برد رو و Reset کنیم کل اطلاعات برد پاک بشه و دوباره پنل از اول بالا بیاد (برای انجام اینکار باید دکمه BOOT رو روش کلیک کنیم و بعدش رمزو بزنیم تا واقعا ریست کنه برد رو)

دکمه logout هم بزار
Change AP Password
Change Panel Password
هم بزار
ولی به هیچ عنوان نزار کاربر ssid وای فای رو عوض کنه
اس اس ایدی وای فای ما باشه bOX-Utility

توجه کن پروژه ما دو تا فریمور داره
یدونه فریمور خونه و دیگری سایت

پنل هردوشون متفاوته یه سری آپشنا داره اون یکی نداره و بلعکس ولی دسترسی کانکشنون یکیه.

در صورت ارور و قطعی در کانکشن هم توی پروژه ما باید watchdog و اپشنی باشه که ریکانکت بشه اتصالمون برای هردو فریمور...


و یک سری چیزای دیگه داداش
ما داریم از platform io برای پروگرام زدن و کتابخونه ها استفاده میکنیم
داداش از بهترین کتابخونه ها برای این کار استفاده کن

یکی بود به اسم khoih-prog اگه اشتباه ننوشته باشم
از پروژه های اون استفاده کن. ولی از اکزمپل و مثال هاش هم طرح ایده بگیر کلا میگم از بهترین لایبری ها استفاده کن. 

ولی یه چیزیو دقت کن
من از chatgpt پرسیدم گفت بهترین لایبری webserver esp32 w5500 هست از khoih prog که کامل ترینه ، نمیدونم چیکار کنم ببین بهترین لایبری از نظر خودت رو بررسی کن 

یکسری مشکلات رو بگم
ما توی w5500 از نظر اتصال پورت و پین ها هیچ مشکلی نداریم داداش
ولی توی دریافت DHCP مشکل هست که ارتباط رو بهم میزنه
من یکی از مثال های این طرف که بالا اشاره  بهش کردمو تست کردم ببین همه پینارو تست گرفت نتیجس عالی بود و میفهمه وقتی من اترنت رو جدا میکنم یا نه (توی SERIAL LOG  پلتفرم آی او VSCODE)

و وقتی اینو میزنم به سیستمم یه ipconfig میگیرم بهم یه ای پی AIPA نشون میده مثلا 169.243.37.50 همچین شکلی که ویندوز این ایپی رندوم رو میده چون نمیتونه ای پی از دستگاه ما بگیره و ساب نت مسک هم هست 255.255.0.0 و گیت وی هم نداشتیم

این مشکل توی پروژه های قبلیمون بود...
امیدوارم تو حلش کنی.


**نکته بسیار مهم W5500 به هیچ عنوان نیاز به وب سرور ندارد و فقط کارش ارسال و دریافت و به صورت کلی انتقال تانل کانکشن اترنت LMT به صورت تحت شبکه ای پی ما به سمت سیستم ما توی خونه هستش از طریق باکس توی خونمون. منظورم از اینکه وب سرور نداره یعنی نباید پنل توی شبکه لن باز بشه ولی اکسس پوینت باکس ما چرا...

نکته بعدی:
چراغ آبی رنگ esp32 که LED 2 هستش نقش مهمی توی اکوسیستم کار ما داره

این نوع نشون دهی رنگ آبی در برد نشون دهنده وضیعت هست (هردو فریمور)
رنگ قرمز که همیشه روشنه هیچی...
وقتی روشن میشه برد = به مدت ۳ ثانیه چراغ آبی باید روشن باشه

بعد از اون ۳ ثانیه باید فریمور رو لود کنی به هر نحوی که هست و اکسس پوینت توی اون لحظات فعال میشه و اینترفیس ماژول اترنت ما که = باید با سرعت بسیار بالا کمتر از نیم ثانیه چراغ آبی روشن و خاموش بشه به مدت ۵ ثانیه

بعدش چراغ آبی بعد از لود شدن فریمور و همچی باید به شکل pulse مانند به صورت سریع آروم روشن بشه و خاموش همینطوری تا ۵ ثانیه

بعدش وقتی به وای فای یا اکسس پوینت esp32 وصل شدیم
پالس آبی از حالت سریع بیاد روی آروم یعنی آروم روشن و خاموش شدن و این حالت مدتی نداره تا وقتی که کاربر تانل رو استارت بزنه که رنگ پالس آبی باید ۱۰ ثانیه روشن بمونه آروم خاموش و روشن بشه (لوپ باشه) تا وقتی کانکشن تانل وصله وگرنه باید پالس آروم آبی باشه

اسم SSID ما: (نباید تغییر کنه توی پنل و به هر نحوی فقط توی کد هست!)
LSKW-EUABU-MSX1
رمز: lsmodadmin

و رمز پنل:
lsmod-superadmin
یبار شده بود توی کد های قدیمیت بعد وارد کردن پسورد هندل نمیشد حرکت بعدی و پنل ریلود میشد ، راستی یه نکته دیگه توی پنل لاگین زیرش پایین صفحه یه متن باید باشه (توجه کن کل پنل لاگین و پنل ادمین ما باید با گوشی و سیستم رسپانسیو باشه) ورژن و اینا نوشته شده باشه مثلا
LavaShell Panel - Firmware Version: H0#1 - HOME
Build 1 - CREATED BY MOEIN NIKCHEHRE

بیشترین مشکل رو توی اتصال به w5500 و اتصال اون به سیستم توی خونمون داریم که میترسم توی اتصال ما به تجهیزات اریکسون LMT که فقط قراره کانکشن اونو تانل کنیم...

بسیار مشکلاتی زیادی توی این زمینه داشتیم ولی پین و کابل های جامپر درست وصل شدن نمیدونم مشکل از لایبری ها بود یا نه که من اینارو با تو تست کرده بودم قبلا شایدم مشکل از کد تو بوده
Ethernet
Ethernet2
Ethernet3
webserver esp32 w5500
UIPEthernet
EthernetENC
SIP و ...

HTTPS (self-signed certificate + key embedded)

Login screen (modern HTML panel)

JWT auth

Per-IP allow-list firewall

/api/logs endpoint streaming last 200 serial lines

Full responsive HTML (no placeholders)

ولی احساس میکنم spi و یه چیزی بود spisfs بود اینا نیاز بشه ولی از example های webserver esp32 w5500 هم استفاده شدیدی داشته باش

ولی یه چیزی ببین داش اول این لایبری قابلیت تانل و انجام کاری که ما میخوایم با اترنت رو انجام بدیم رو داره ؟

وقتی هم وصلش میکنم به سیستم دیاگناستیک رو میزنم میزنه برام
Ethernet doesnt have a vaild IP Configuartion
یه جای کار میلنگه ما میخوایم ای پی به صورت static گرفته نشه و به صورت dhcp باشه!
توی یکی از کدای وب سرور اینو نوشته بود (توجه بسیار مهم باید تصمیم بگیری از کدوم لایبری و کامل ترین سریع ترین و قدرتمند ترین و امنیت دار ترین استفاده کنی، ما نمیخوایم اترنت وب سرور ما باشه! ما میخوایم از اون کانکشن تانل بدیم) که متغیر هاش با ما فرق میکنه (با کد های قبلی که برام نوشته بودی و بد بودن)
حالت سرچ رو برات فعال میکنم کل plotfrom io رو قسمت رجیستری لایبری ها اکزمپل هارو نگاه کنی

مثلا توی کد فقط برای لایبری های اترنت فقط webserver esp32 w5500 استفاده شده (به جز این منظورمه و گرنه لایبری های دیگه داریم که ربطی به اترنت ندارن ) و جالبه از spi استفاده نکرده نمیدونم باید استفاده کرد یا نه 
و mac address این شکلی چند تا خط نوشته بود
0xDE 0xAD ....

بعد مثلا یجا اینطوری بود
IPAddress myIP(192, 168, 2, 232);
IPAddress myGW(192, 168, 2, 1);
IPAddress mySN(255, 255, 255, 0);

به صورت کلی میگم داش این نحوه اون کد مثال خود لایبری بود داش...
تمامی نکاتی که بهت گفتم رو دقیق رعایت کن به صورت کامل باید طبق چیزیایی که گفتم باشه و با دقت کد رو بنویس و هنگ نکن داش!